# note記事自動作成・自動アップロード システム設計書

**作成日**: 2026-05-09  
**バージョン**: 4.0

---

## 1. システム概要

### 1.1 目的

チャット UI でユーザーから変数を収集し、テンプレートに自動埋め込んだ上でマルチエージェントシステム（MAS）が記事を生成・品質チェック・note.com へアップロードする。任意のテーマ・読者・目的に転用可能な設計とする。

### 1.2 処理の全体像

```
Phase 0: 変数収集・テンプレート埋め込み（Config Agent）
  └─ チャット UI で 13問に回答 → 約34変数を自動導出 → runtime/ に展開

Phase 1〜2: 記事生成（Parser / Research / Writing / Quality Check Agent）
  └─ runtime/ のファイルを参照して記事を生成・品質評価

Phase 3: 人間レビュー＆アップロード（Upload Agent + 3つの HITL）
  └─ 人間の承認を得て note.com に公開
```

### 1.3 インプット・アウトプット定義

| 区分 | パス | 内容 | 操作主体 |
|------|------|------|---------|
| **Templates** | `templates/` | 変数プレースホルダー入りの雛形4種 + 変数定義マスタ | 開発者が整備（ユーザーは触らない） |
| **Runtime** | `runtime/` | 変数を埋め込み済みの実行用ファイル群 | Config Agent が自動生成 |
| **Runtime Input** | `runtime/input/記事構成.md` | チャットで記入されたシリーズ構成表 | Config Agent が生成 |
| **Runtime Prompt** | `runtime/prompt/記事作成プロンプト.md` | 埋め込み済み執筆プロンプト | Config Agent が生成 |
| **Runtime Prompt** | `runtime/prompt/品質チェックプロンプト.md` | 埋め込み済み品質チェックプロンプト | Config Agent が生成 |
| **Runtime Output** | `runtime/output/記事構造.md` | 埋め込み済み文体・章構成基準 | Config Agent が生成 |
| **Variables** | `runtime/variables.json` | 全変数のキーバリュー（CORE + DERIVED） | Config Agent が生成 |
| **Existing Sample** | `output/AI経営実装シリーズ_第1回.docx` | AI経営実装版の完成記事サンプル（参照用） | 既存ファイル |

### 1.4 設計上の重要前提

- **ユーザーはファイルを直接編集しない**: 変数はすべてチャット UI で収集し、Config Agent がテンプレートに埋め込む。
- **骨子はゼロから作らない**: 記事の構成（タイトル・結論・章立て）は Config Agent が UI で記入を促し、`runtime/input/記事構成.md` に保存する。
- **1記事ずつ処理**: シリーズは複数回あるが、MAS は1回分を対象として起動する。
- **テーマ非依存**: 13問に答えるだけで任意テーマ・任意読者の記事シリーズに対応できる。

---

## 2. システムアーキテクチャ

```
╔═══════════════════════════════════════════════════════════════════════╗
║                     ユーザー（チャット UI）                            ║
║  Phase 0: 13問に回答   │  Phase 1〜2: 進捗確認   │  Phase 3: レビュー・承認 ║
╚══════════════════════╤══════════════════════════╤════════════════════════╝
                       │ 変数入力                  │ HITL①②③
                       ▼                          │
          ┌────────────────────────┐              │
          │     Config Agent       │              │
          │  ① 13問でCORE変数収集  │              │
          │  ② DERIVED変数を自動導出│              │
          │  ③ サマリー提示→確認   │              │
          │  ④ templates/ に埋め込み│              │
          │     → runtime/ に出力  │              │
          └────────────┬───────────┘              │
                       │ runtime/ 生成完了          │
                       ▼                          │
          ┌────────────────────────────────────────┴──────────────────┐
          │                    Orchestrator Agent                      │
          │        全体制御・エージェント間データ受け渡し・HITL 制御    │
          └──┬────────────────┬──────────────────────┬────────────────┘
             ↓                ↓                      ↓
       ┌──────────┐   ┌──────────────┐   ┌──────────────────────────┐
       │ Parser   │   │  Research    │   │    Writing Agent         │
       │ Agent    │──►│  Agent       │──►│  runtime/prompt/         │
       │ 記事構成 │   │  情報収集    │   │  記事作成プロンプト参照  │
       │ 解析     │   │  （章別並列）│   │  9章構成で執筆           │
       └──────────┘   └──────────────┘   └────────────┬─────────────┘
                                                       │ フィードバック（不合格時）
                                                       ▼
                                         ┌─────────────────────────┐
                                         │   Quality Check Agent   │
                                         │   runtime/prompt/       │
                                         │   品質チェックプロンプト│
                                         │   10項目×10点 = 100点  │
                                         └─────────────┬───────────┘
                                                       │
                                          ┌────────────┴──────────┐
                                          │ スコア ≥ 85点?         │
                                          └────────────┬──────────┘
                                               YES     ↓
                                         ┌─────────────────────────┐
                                         │  [HITL-①] 人間レビュー  │
                                         └─────────────┬───────────┘
                                               承認     ↓
                                         ┌─────────────────────────┐
                                         │  [HITL-②] Upload 承認   │
                                         └─────────────┬───────────┘
                                               承認     ↓
                                         ┌─────────────────────────┐
                                         │     Upload Agent        │
                                         │     note-mcp 経由投稿   │
                                         └─────────────┬───────────┘
                                                       ↓
                                         ┌─────────────────────────┐
                                         │  [HITL-③] 公開最終確認  │
                                         └─────────────┬───────────┘
                                                       ↓
                                         ┌─────────────────────────┐
                                         │    note.com（公開）      │
                                         └─────────────────────────┘
```

---

## 3. エージェント定義

### 3.0 Config Agent（変数収集・テンプレート埋め込みエージェント）

**役割**: チャット UI でユーザーから変数を収集し、テンプレートに埋め込んで MAS が読めるファイル群（`runtime/`）を生成する。**MAS 起動前の Phase 0 専任エージェント。**

**責務**:
- `templates/variables_definition.md` を参照し、CORE 変数13問をグループ別にユーザーへ質問する
- 回答を受け取るたびに `runtime/variables.json` に記録する
- 13問完了後、DERIVED 変数（約34個）を `variables_definition.md` の導出ルールに従って自動生成する
- 設定サマリーをユーザーに提示し「確認 / 修正」を待つ
- 確認後、テンプレート4ファイルへ変数を一括置換して `runtime/` に保存する
- 記事シリーズの全回分構成をチャットで記入するよう促し、`runtime/input/記事構成.md` に保存する
- 既存プロジェクト（`runtime/variables.json` が存在する場合）は設定を再利用するか確認する

**入力**: ユーザーのチャット入力  
**出力**: `runtime/` フォルダ（variables.json + 4つの実行用ファイル）

#### チャット UI の質問シーケンス（13問）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  グループ 1/5：基本情報
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q1. このシリーズの名前を教えてください
    例: AI経営実装シリーズ
    > ___

Q2. 全何回のシリーズですか？（数値）
    例: 10
    > ___

Q3. 記事のテーマ・ドメインを一言で教えてください
    例: AI組織実装・AI経営実装
    > ___

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  グループ 2/5：ターゲット設定
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q4. 記事を書く「著者」のペルソナを教えてください
    例: BtoBコンサルティング会社の編集責任者兼、経営層向けコンテンツマーケティングの専門家
    > ___

Q5. 対象読者を役職・職種で列挙してください（カンマ区切り）
    例: CEO、COO、CFO、DX担当役員、経営企画、事業部長
    > ___

Q6. 上記読者の総称を一言で（例: 経営層、マーケ担当者、中小企業経営者）
    > ___

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  グループ 3/5：目的・ゴール
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q7. 記事を読んだ読者に最終的にとってほしいアクションを教えてください
    例: AI経営実装コンサルティングの相談・受注につなげること
    > ___

Q8. 記事で訴求するサービスまたは商品の名称を教えてください
    例: AI経営実装コンサルティング
    > ___

Q9. 読者が抱える核心的な問題・痛みを一言で表してください
    例: 使っているのに成果が出ない
    > ___

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  グループ 4/5：コンテンツ設計
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q10. 記事に入れる独自フレームワーク・診断軸の例を教えてください（複数可、スラッシュ区切り）
     例: AI成果化成熟度診断 / 90日実装ロードマップ / AI PoC学習成熟度診断
     > ___

Q11. 具体例として登場させる業務機能・部門を列挙してください（カンマ区切り）
     例: 営業、契約、法務、経理、人事、問い合わせ対応
     > ___

Q12. 引用する信頼できる情報源を列挙してください（カンマ区切り）
     例: BCG、PwC、JUAS、NRI、企業公式発表
     > ___

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  グループ 5/5：CTA 設計
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q13. 初回相談・診断で読者に提供できる価値を具体的に列挙してください（スラッシュ区切り）
     例: 現在のAI活用状況の棚卸し / 優先的に実装すべき業務の特定 / 経営会議で追うべきKPI / 次の90日アクション
     > ___
```

#### DERIVED 変数の自動導出（確認後にユーザー修正可）

13問完了後、Config Agent は以下を自動生成して設定サマリーを提示する。

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  設定サマリー（自動導出された変数の確認）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▼ CORE 変数（ご入力内容）
  シリーズ名    : AI経営実装シリーズ（全10回）
  テーマ        : AI組織実装・AI経営実装
  対象読者      : CEO、COO、CFO、DX担当役員（総称: 経営層）
  記事のゴール  : AI経営実装コンサルの相談・受注につなげること
  核心的な問題  : 使っているのに成果が出ない
  ...

▼ DERIVED 変数（自動生成）
  読者の感情1   : 「これは自社の経営課題だ」
  読者の感情2   : 「このまま放置するとAI投資が成果化しない」
  中心的な問い  : AI活用の問題を、経営成果・投資判断・組織設計…として描けているか
  不合格パターン: AIツール紹介で終わっている / プロンプト術の記事 …
  良いCTA例     : 初回診断では、現在のAI活用状況を棚卸しし…
  ...（全34変数）

[1] このまま進む
[2] 特定の変数を修正する → 変数名と修正値を入力
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### runtime/ への出力

確認後、Config Agent は以下を生成する：

```
runtime/
├── variables.json                  ← CORE + DERIVED 全変数のキーバリュー
├── input/
│   └── 記事構成.md                 ← チャットで記入されたシリーズ全回分の構成表
├── prompt/
│   ├── 記事作成プロンプト.md       ← 変数埋め込み済みの執筆プロンプト
│   └── 品質チェックプロンプト.md   ← 変数埋め込み済みの評価プロンプト
└── output/
    └── 記事構造.md                 ← 変数埋め込み済みの文体・章構成基準
```

**入力**: ユーザーのチャット入力 + `templates/` フォルダ  
**出力**: `runtime/` フォルダ一式

---

### 3.1 Orchestrator Agent（統括エージェント）

**役割**: 全体のワークフロー制御、エージェント間のデータ受け渡し、状態管理、HITL ポイントの制御

**責務**:
- ユーザーから「第N回」の指定を受け取り、処理を開始
- `input/記事構成.docx` の読み込みと該当回のデータ取得を Parser Agent に依頼
- 各エージェントの起動・停止・リトライ制御
- スコア閾値（85点）の判定と分岐処理
- HITL ポイントでのユーザー通知・待機・入力処理
- エラーハンドリング

**入力**: 対象回数（例: 第1回）  
**出力**: 公開済みURLまたは終了報告

---

### 3.2 Parser Agent（入力解析エージェント）

**役割**: `input/記事構成.docx` から対象回の記事仕様を構造化して取り出す

**責務**:
- `input/記事構成.docx` の該当行を読み込む
- 6項目（回・記事名・結論・主要読者・章立て・次回へのつながり）を構造化JSONに変換
- 章立てを個別セクションに分解して Research Agent が使える形式に整形

**入力**: 対象回数  
**出力**: 記事仕様JSON

```json
{
  "episode": 1,
  "title": "日本企業のAI導入は、なぜ「使っているのに成果が出ない」のか",
  "conclusion": "AI課題の本質は導入の遅れではなく、成果化の遅れである",
  "target_readers": ["CEO", "COO", "DX担当役員", "経営企画"],
  "sections": [
    { "order": 1, "heading": "AI活用は広がっているが成果実感は薄い", "keywords": [] },
    { "order": 2, "heading": "問うべきは「導入したか」ではなく「成果に変わったか」", "keywords": [] },
    { "order": 3, "heading": "日本企業はAIに無関心なのではない", "keywords": [] },
    { "order": 4, "heading": "個人利用と組織成果の間にある断絶", "keywords": [] },
    { "order": 5, "heading": "AI導入の本質課題は「組織実装」である", "keywords": [] },
    { "order": 6, "heading": "経営が最初に持つべき問い", "keywords": [] }
  ],
  "next_episode_bridge": "成果化を語るには、まず「成果とは何か」を定義する必要がある"
}
```

---

### 3.3 Research Agent（情報収集エージェント）

**役割**: 記事仕様の各セクションに必要な情報・データ・事例を収集する

**責務**:
- 各セクションの見出しと記事テーマをもとに WebSearch で情報収集
- 経営層向けコンテンツに適した情報源（BCG・PwC・JUAS・NRI・企業公式発表 など）を優先的に取得
- 統計データの出典名・調査名・発表年を記録（ファクト信頼性の確保）
- 一次情報で確認できない企業事例は「断定できない情報」としてフラグを立てる
- 各セクションを**並列処理**で同時収集し、処理時間を短縮

**入力**: 記事仕様JSON（Parser Agent の出力）  
**出力**: セクション別情報パッケージJSON

```json
{
  "section_order": 1,
  "heading": "AI活用は広がっているが成果実感は薄い",
  "collected_info": [
    {
      "content": "NRIの2025年調査では、生成AIを「導入済み」と回答した企業の割合が57.7%に達した",
      "source": "NRI「ユーザー企業のIT活用実態調査2025」",
      "reliability": "high",
      "is_verifiable": true,
      "note": ""
    }
  ],
  "statistics": [],
  "case_studies": []
}
```

**品質基準への対応**:
- 記事作成プロンプトの「ファクトが安全である」基準に準拠
- `出典名・調査名・発表年` を必ず収集
- 断定・誇張表現（「圧倒的」「必ず」「実現した」）の元ネタ確認

---

### 3.4 Writing Agent（記事執筆エージェント）

**役割**: 記事仕様・収集情報・プロンプトをもとに記事本文を執筆する

**責務**:
- `prompt/記事作成プロンプト.docx` の指示に全面準拠して執筆（テーマ非依存化済みプロンプト）
- `output/記事構造.md`（または `output/AI経営実装シリーズ_第1回.docx`）の文体・章構成を基準として踏襲
- 下記の**必須9章構成**で記事を組み立てる（`templates/output_記事構造_template.md` で定義）
- Quality Check Agent のフィードバックを受けて再執筆（最大3回）

**入力**: 記事仕様JSON + セクション別情報パッケージ + 記事作成プロンプト + 文体基準ファイル  
**出力**: Markdown 形式の記事本文

#### 必須9章構成（`output_記事構造_template.md` 準拠・テーマ非依存）

| 章 | 汎用ラベル | AI経営実装版での内容 | 執筆上の注意 |
|----|-----------|-------------------|------------|
| 1. タイトル | `{{TITLE_KEYWORDS}}` を含む | 経営課題・投資対効果・成果化・組織設計・ガバナンス | ターゲット読者が気になるリスク・判断を示す |
| 2. リード文 | 読者の痛み（`{{PAIN_POINT_EXAMPLES}}`）から入る | 「使っているのに成果が出ない」類の違和感 | 記事の問いを明確にして締める |
| 3. 課題の経営・事業化 | `{{MGMT_TOPIC_AREA}}` に接続 | 投資・ROI・機会損失・ガバナンス・説明責任 | 現場課題ではなく上位課題として描く |
| 4. 構造的原因の分解 | `{{STRUCTURAL_DIMENSIONS}}` として整理 | 業務プロセス・KPI・責任範囲・会議体・データ | 個人・現場の問題にしない |
| 5. 独自フレームワーク | `{{FRAMEWORK_EXAMPLES}}` を参考に提示 | AI成果化成熟度診断 / 90日実装ロードマップ など | 表またはチェックリスト形式。資料っぽくなりすぎない |
| 6. 実務例 | `{{BUSINESS_FUNCTIONS}}` に接続 | 営業・法務・経理・人事・問い合わせ対応 | 「どの工程」「どのKPI」まで踏み込む |
| 7. 読者が取るべきアクション | `{{ACTION_ITEMS}}` | 経営会議指標・優先順位・ガバナンス設計・90日ステップ | |
| 8. まとめ | 冒頭の問いに戻り `{{SUMMARY_REFRAME}}` で回収 | 「AIの問題」ではなく「経営設計の問題」 | |
| 9. CTA | `{{CTA_DIAGNOSIS_ITEMS}}` を含む | 初回診断で整理する4論点（現状把握・優先業務・KPI・90日アクション） | `{{CTA_BAD_EXAMPLE}}` で終わらせない |

#### テンプレートが定義する文体基準（`output_記事構造_template.md` より）

| 使って良い表現 | 避けるべき表現 |
|-------------|--------------|
| 「〜の構造を解剖する」「〜に踏み込む」 | 「圧倒的多数」「必ず〜できる」 |
| 「注目すべきは数字ではなく、そこに至る設計だ」 | 「劇的に改善」「本当の理由」 |
| 「問われているのは〜ではなく〜か、である」 | 「お気軽にご相談ください」 |

---

### 3.5 Quality Check Agent（品質評価エージェント）

**役割**: `prompt/品質チェックプロンプト.docx` の指示に基づき、完成記事を多角的に評価する

**責務**:
- 評価10項目を10点満点でスコアリング（合計100点）
- 加点・減点ルールを適用した最終スコアの算出
- 閾値（85点）との比較判定
- 不合格時: 具体的な修正フィードバックを生成して Writing Agent に返却
- 合格時: Human-in-the-Loop ① 用の評価レポートを生成

**入力**: Markdown 形式の記事本文 + 記事仕様JSON + 品質チェックプロンプト  
**出力**: 評価レポートJSON

#### 評価10項目（品質チェックプロンプト準拠）

| # | 評価項目 | 配点 | 主な評価観点 |
|---|---------|------|------------|
| 1 | タイトル | 10点 | 経営課題と成果が明確でクリックしたくなるか |
| 2 | 冒頭の掴み | 10点 | 経営層の痛みを具体的に突いているか |
| 3 | 経営層への刺さり | 10点 | CEO/COO/CFO/DX役員それぞれが読む理由があるか |
| 4 | 問題設定の鋭さ | 10点 | 「使っているのに成果が出ない」構造を鋭く突いているか |
| 5 | 経営インパクト | 10点 | ROI・機会損失・重複投資・競争優位に接続しているか |
| 6 | 独自性 | 10点 | 独自の診断軸・フレーム・方法論があり相談したくなるか |
| 7 | 具体性 | 10点 | 業務工程・KPI・責任者・判断基準まで具体的か |
| 8 | ファクト信頼性 | 10点 | 出典が正確で断定と解釈が分かれているか |
| 9 | 読みやすさ | 10点 | 章タイトルだけで論旨が追え短時間で理解できるか |
| 10 | 受注導線 | 10点 | 本文の問題提起からCTAまで自然につながり初回診断の価値が明確か |
| | **合計** | **100点** | |

#### 減点ルール

| 該当条件 | 減点 |
|---------|------|
| 出典不明の数字を使っている | -5点 |
| 出典の内容を超えて断定している | -5点 |
| AIツール紹介に寄りすぎている | -10点 |
| プロンプト術の記事になっている | -10点 |
| 経営層ではなく担当者向けに見える | -10点 |
| CTAが弱い、またはない | -10点 |
| 独自フレームがない | -8点 |
| 現場を責める表現がある | -5点 |
| 表が多すぎて記事として読みにくい | -3点 |
| 章タイトルだけで論旨が追えない | -5点 |

#### 加点ルール

| 該当条件 | 加点 |
|---------|------|
| 冒頭で経営層の痛みを強く突いている | +3点 |
| CFO/COOにも刺さる投資・業務設計論点がある | +3点 |
| 独自診断軸がCTAと接続している | +5点 |
| 「放置すると何を失うか」が明確 | +3点 |
| 中止判断・優先順位付け・横展開など経営判断に踏み込んでいる | +3点 |
| 初回診断で得られる成果物が具体的 | +5点 |

#### 評価レポートJSON

```json
{
  "total_score": 87,
  "passed": true,
  "threshold": 85,
  "breakdown": {
    "title":           { "score": 9,  "max": 10, "feedback": "..." },
    "opening":         { "score": 9,  "max": 10, "feedback": "..." },
    "exec_appeal":     { "score": 8,  "max": 10, "feedback": "..." },
    "problem_setting": { "score": 9,  "max": 10, "feedback": "..." },
    "mgmt_impact":     { "score": 9,  "max": 10, "feedback": "..." },
    "originality":     { "score": 8,  "max": 10, "feedback": "..." },
    "specificity":     { "score": 8,  "max": 10, "feedback": "..." },
    "fact_reliability":{ "score": 9,  "max": 10, "feedback": "..." },
    "readability":     { "score": 9,  "max": 10, "feedback": "..." },
    "conversion":      { "score": 9,  "max": 10, "feedback": "..." }
  },
  "deductions": [
    { "rule": "表が多すぎて読みにくい", "points": -3 }
  ],
  "additions": [
    { "rule": "独自診断軸がCTAと接続している", "points": 5 }
  ],
  "strengths": ["強み1", "強み2", "強み3"],
  "weaknesses": ["弱点1（経営層に刺さらない可能性）"],
  "fact_check_notes": ["出典XXの表現が危うい → 修正案"],
  "red_line_examples": [
    { "current": "現状の文章", "revised": "修正案" }
  ],
  "cta_suggestion": "改善CTAの提案文",
  "priority_fixes": ["最優先修正1", "最優先修正2"],
  "final_judgment": {
    "publishable_as_is": false,
    "min_fixes_required": "修正必須箇所",
    "expected_score_after_fix": 92
  },
  "human_review_summary": "人間向け要約（合格時のみ生成）"
}
```

---

### 3.6 Upload Agent（アップロードエージェント）

**役割**: 人間の承認後に note-mcp を通じて記事を投稿する

**責務**:
- 認証状態の確認（`note_check_auth`）
- 承認済み記事を下書き保存（`create_draft`）
- 下書きプレビューURLをユーザーに提示
- ユーザーの最終公開承認後のみ `publish_article` を実行
- アップロード結果の確認と報告

**入力**: 承認済みMarkdown記事 + 投稿設定（タイトル・タグ）  
**出力**: 公開済み記事URLまたはエラー

---

## 4. 業務フロー

### 4.1 全体フロー図

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Phase 0: 変数収集・テンプレート埋め込み
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ユーザー] 「記事を作成したい」とチャットに入力
     ↓
[Config Agent] 既存の runtime/variables.json があるか確認
     ├── あり → 「前回の設定を使いますか？」とユーザーに確認
     │          YES → Phase 1 へスキップ
     │          NO  → 以下の新規収集フローへ
     └── なし → 以下の新規収集フローへ
     ↓
[Config Agent] チャット UI でグループ別に 13問を質問（CORE 変数収集）
     グループ1: 基本情報（シリーズ名・回数・テーマ）
     グループ2: ターゲット（著者ペルソナ・対象読者・総称）
     グループ3: 目的・ゴール（記事目的・サービス名・核心問題）
     グループ4: コンテンツ設計（フレームワーク・業務機能・情報源）
     グループ5: CTA（初回診断で提供する価値）
     ↓
[Config Agent] DERIVED 変数（約34個）を自動導出
     ↓
[Config Agent] 設定サマリーを提示
     ┌──────────────────────────────────┐
     │ [1] このまま進む                  │
     │ [2] 特定の変数を修正する          │
     └──────────────────────────────────┘
     ↓ 確認
[Config Agent] templates/ の4ファイルに変数を一括置換 → runtime/ に保存
[Config Agent] 記事シリーズ全回分の構成入力をチャットで促す
               → runtime/input/記事構成.md に保存
     ↓ runtime/ 生成完了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Phase 1〜2: 記事生成（1回分を対象に起動）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ユーザー] 「第N回を処理して」と指示
     ↓
[Orchestrator] 処理開始・認証確認（note_check_auth）
     ↓
[Parser Agent] runtime/input/記事構成.md から第N回を読み込み、記事仕様JSONを生成
     ↓
[Research Agent] 各セクションを並列で情報収集（WebSearch）
     ↓（全セクション完了後）
[Writing Agent] runtime/prompt/記事作成プロンプト.md + 記事仕様 + 収集情報 → 記事執筆（必須9章構成）
     ↓
[Quality Check Agent] 品質チェックプロンプト → 10項目スコアリング + 加減点
     ↓
  スコア < 85点?
    ├── YES → フィードバックを付けて Writing Agent へ差し戻し（最大3回）
    │          3回失敗 → Orchestrator がユーザーへ報告し終了
    └── NO（85点以上）
              ↓
         ╔══════════════════════════════════════════════════╗
         ║  Human-in-the-Loop ①  記事レビュー              ║
         ║  ・総合スコア（例: 87点）提示                   ║
         ║  ・10項目の内訳提示                             ║
         ║  ・強み・弱点・ファクト注意・赤入れ例を提示     ║
         ║  ・最優先修正リストを提示                       ║
         ║  ・記事本文（全文）を提示                       ║
         ║  [承認する / 修正を依頼する / 却下する]         ║
         ╚══════════════════════════════════════════════════╝
              ↓ 修正依頼 → コメントを Writing Agent に渡し再執筆 → Quality Check へ
              ↓ 却下 → 終了
              ↓ 承認
         ╔══════════════════════════════════════════════════╗
         ║  Human-in-the-Loop ②  アップロード承認          ║
         ║  ・投稿タイトル・タグ・公開範囲を提示           ║
         ║  「note に下書き保存してアップロードしますか？」 ║
         ║  [はい / いいえ]                                ║
         ╚══════════════════════════════════════════════════╝
              ↓ はい
         [Upload Agent] note_check_auth → create_draft（下書き保存）
              ↓
         下書きプレビューURLをユーザーに提示
              ↓
         ╔══════════════════════════════════════════════════╗
         ║  Human-in-the-Loop ③  公開最終確認              ║
         ║  ・プレビューを確認してください                 ║
         ║  「公開しますか？下書きのままにしますか？」     ║
         ║  [公開する / 下書きのまま]                      ║
         ╚══════════════════════════════════════════════════╝
              ↓ 公開する
         [Upload Agent] publish_article（公開実行）
              ↓
         公開済みURLをユーザーに報告 → 完了
```

### 4.2 フェーズ別詳細

#### Phase 0: 変数収集・テンプレート埋め込み（Config Agent）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 0-1 | ユーザー | 「記事を作成したい」とチャットに入力 |
| 0-2 | Config Agent | `runtime/variables.json` の存在確認。あれば再利用を提案 |
| 0-3 | Config Agent | `templates/variables_definition.md` を参照し、CORE 変数13問をグループ順に質問 |
| 0-4 | ユーザー | 13問に順次回答（チャット形式） |
| 0-5 | Config Agent | 回答を受け取るたびに `runtime/variables.json` に追記 |
| 0-6 | Config Agent | 13問完了後、導出ルールに従い DERIVED 変数（約34個）を自動生成 |
| 0-7 | Config Agent | 設定サマリー（CORE + DERIVED 全変数）をユーザーに提示 |
| 0-8 | ユーザー | [確認 / 特定変数の修正] を選択。修正の場合は変数名と新値を入力 |
| 0-9 | Config Agent | `templates/` の4ファイルを読み込み、`{{変数名}}` を一括置換して `runtime/` に保存 |
| 0-10 | Config Agent | 記事シリーズ全回分の構成入力をチャットで促す（回ごとに: タイトル・結論・読者・章立て・次回接続） |
| 0-11 | Config Agent | 入力を `runtime/input/記事構成.md` に保存 |
| 0-12 | Config Agent | 「セットアップ完了。`第N回を処理して` と指示してください」とユーザーに通知 |

#### Phase 1: 起動・認証確認

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 1-1 | ユーザー | 「第N回を作成して」と指示 |
| 1-2 | Orchestrator | `runtime/variables.json` の存在確認（Phase 0 完了チェック） |
| 1-3 | Orchestrator | note-mcp 認証状態を確認（`note_check_auth`） |
| 1-4 | Orchestrator | 未認証の場合 `note_login` でブラウザ認証を促す |
| 1-5 | Orchestrator | Parser Agent を起動 |

#### Phase 2: 記事仕様の読み込み（Parser Agent）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 2-1 | Parser Agent | `runtime/input/記事構成.md` を読み込む |
| 2-2 | Parser Agent | 対象回（第N回）のデータを特定 |
| 2-3 | Parser Agent | 6項目を構造化JSONに変換（章立てをセクション配列に分解） |
| 2-4 | Orchestrator | 記事仕様JSONを受け取り、Research Agent を起動 |

#### Phase 3: 情報収集（Research Agent・並列）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 3-1 | Research Agent×N | 各セクション見出しで WebSearch 実行（セクション数分並列） |
| 3-2 | Research Agent | `runtime/variables.json` の `TRUSTED_SOURCES` に記載の情報源を優先取得 |
| 3-3 | Research Agent | 出典名・調査名・発表年を記録 |
| 3-4 | Research Agent | 一次情報未確認の事例に「未確認フラグ」を付与 |
| 3-5 | Orchestrator | 全セクション完了後に統合して Writing Agent へ渡す |

#### Phase 4: 記事執筆（Writing Agent）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 4-1 | Writing Agent | `runtime/prompt/記事作成プロンプト.md` を参照（変数埋め込み済み） |
| 4-2 | Writing Agent | `runtime/output/記事構造.md` を文体基準として参照 |
| 4-3 | Writing Agent | 必須9章構成（タイトル〜CTA）で記事を執筆 |
| 4-4 | Writing Agent | 減点対象の表現・構成を自己チェックして回避 |
| 4-5 | Writing Agent | 次回連載接続文を末尾に追記（記事仕様JSONの `next_episode_bridge` を使用） |

#### Phase 5: 品質チェック（Quality Check Agent）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 5-1 | Quality Check Agent | `runtime/prompt/品質チェックプロンプト.md` を参照（変数埋め込み済み） |
| 5-2 | Quality Check Agent | 10項目を10点満点でスコアリング |
| 5-3 | Quality Check Agent | 加点・減点ルールを適用して最終スコアを算出 |
| 5-4 | Quality Check Agent | 評価レポートJSON（強み・弱点・赤入れ例・CTA改善案・最優先修正リスト）を生成 |
| 5-5 | Orchestrator | スコア < 85点 → フィードバック付きで Writing Agent へ差し戻し（最大3回） |
| 5-6 | Orchestrator | スコア ≥ 85点 → Human-in-the-Loop ① へ遷移 |

#### Phase 6: 人間レビュー（HITL-①）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 6-1 | Orchestrator | 評価レポート（スコア・内訳・強み・弱点・赤入れ例・CTA改善案）を提示 |
| 6-2 | Orchestrator | 記事本文（全文）を提示 |
| 6-3 | ユーザー | 内容を確認し判断 |
| 6-4 | ユーザー | [承認] / [修正依頼（コメント入力）] / [却下] を選択 |
| 6-5 | Orchestrator | 修正依頼 → コメントを Writing Agent に渡して再執筆 → Phase 5 へ |

#### Phase 7: アップロード承認（HITL-②）

| ステップ | 担当 | 処理内容 |
|---------|------|---------|
| 7-1 | Orchestrator | タイトル・タグ・公開範囲の設定案を提示 |
| 7-2 | ユーザー | 設定を確認し「はい / いいえ」を選択 |

#### Phase 8: note アップロード

| ステップ | 担当 | MCP ツール | 処理内容 |
|---------|------|-----------|---------|
| 8-1 | Upload Agent | `note_check_auth` | 認証状態の最終確認 |
| 8-2 | Upload Agent | `create_draft` | 下書きとして保存 |
| 8-3 | Orchestrator | — | 下書きプレビューURLをユーザーに提示 |
| 8-4 | ユーザー | — | プレビューを確認（HITL-③） |
| 8-5 | Upload Agent | `publish_article` | 公開実行（ユーザー承認後のみ） |
| 8-6 | Orchestrator | — | 公開済みURLを報告して完了 |

---

## 5. データフロー図

### 5.1 テンプレート → プロジェクトファイル（セットアップ時・一度だけ）

```
templates/
  ├── input_記事構成_template.md      変数を埋める    input/記事構成.docx
  ├── prompt_記事作成_template.md    ──────────►    prompt/記事作成プロンプト.docx
  ├── prompt_品質チェック_template.md              prompt/品質チェックプロンプト.docx
  └── output_記事構造_template.md                  output/記事構造.md（文体基準）

  ※ AI経営実装シリーズでは置換済みファイルが既存（templates/ は次テーマ転用時に使用）
```

### 5.2 記事生成フロー（1記事実行時）

```
input/記事構成.docx
    │ 第N回のデータ
    ▼
Parser Agent ──────────────────────────────────► 記事仕様JSON
                                                      │
                              ┌───────────────────────┘
                              │ セクション情報（並列配布）
                   ┌──────────┼──────────┬──────────┐
                   ▼          ▼          ▼          ▼
              Research-1  Research-2  Research-3  Research-N
                   │          │          │          │
                   └──────────┼──────────┴──────────┘
                              │ 情報パッケージ（統合）
                              ▼
prompt/記事作成プロンプト.docx  ──►  Writing Agent  ◄── フィードバック（不合格時）
output/記事構造.md（文体・章構成基準）     │
                                          │ Markdown記事
                                          ▼
prompt/品質チェックプロンプト.docx ──► Quality Check Agent
                                          │
                                 ┌────────┴─────────┐
                                 │ <85点            │ ≥85点
                                 ▼                  ▼
                            フィードバック      評価レポート
                                 │                  │
                                 └─────► [HITL-①] ◄─┘
                                              │ 承認
                                         [HITL-②] アップロード承認
                                              │ はい
                                         Upload Agent
                                         note_check_auth
                                         create_draft
                                              │
                                         [HITL-③] 公開確認
                                              │ 公開する
                                         publish_article
                                              │
                                         note.com（公開）
```

---

## 6. Human-in-the-Loop ポイント一覧

| ポイント | タイミング | ユーザーへの提示内容 | 選択肢 | 次のアクション |
|---------|----------|-------------------|-------|--------------|
| **HITL-①** | 品質チェック合格後 | スコア・10項目内訳・強み・弱点・赤入れ例・CTA改善案・最優先修正リスト・記事全文 | 承認 / 修正依頼 / 却下 | 承認→HITL-② / 修正→再執筆 / 却下→終了 |
| **HITL-②** | アップロード直前 | タイトル・タグ・公開範囲の設定案 | はい / いいえ | はい→下書き保存 / いいえ→終了 |
| **HITL-③** | 下書きプレビュー確認後 | 下書きプレビューURL | 公開する / 下書きのまま | 公開→publish実行 / 下書き→保存のまま終了 |

> **原則**: HITL ポイントでは Claude が自律的にアクションを取らない。必ず人間の明示的な入力を待機する。

---

## 7. MCP サーバー連携仕様

### 7.1 使用する note-mcp ツール

| ツール名 | フェーズ | 用途 |
|---------|---------|------|
| `note_check_auth` | Phase 1, Phase 8 | 認証状態の確認 |
| `note_login` | Phase 1（未認証時） | ブラウザログイン |
| `create_draft` | Phase 8-2 | 下書き保存 |
| `publish_article` | Phase 8-5 | 記事公開（HITL-③ 承認後のみ） |

### 7.2 アップロード時のデータマッピング

| note-mcp パラメータ | 取得元 |
|--------------------|------|
| `title` | 記事仕様JSON の `title`（Parser Agent が記事構成.docx から取得） |
| `body` | Writing Agent が生成した Markdown 本文 |
| `tags` | 記事仕様JSON の `target_readers` から生成（例: "AI経営"・"DX"・"経営層"） |
| `status` | `"draft"`（下書き）→ HITL-③ 承認後に `publish_article` |

---

## 8. エラーハンドリング

| フェーズ | エラーケース | 対処 |
|---------|------------|------|
| Phase 0 | ユーザーが途中で回答をやめた | `runtime/variables.json` に途中まで保存されているため、次回「続きから」として再開できる旨を通知 |
| Phase 0 | DERIVED 変数の自動導出に失敗した | 該当変数をデフォルト値で補完し、サマリー画面でユーザーに手動修正を促す |
| Phase 0 | テンプレートへの置換後に未置換の `{{変数名}}` が残った | 置換漏れ変数を列挙してユーザーに通知。手動入力を求める |
| Phase 1 | `runtime/variables.json` が存在しない | Phase 0 を先に実行するよう促す |
| Phase 1 | Parser Agent が対象回を見つけられない | エラーメッセージとともに処理停止。ユーザーに回数の確認を促す |
| Phase 2 | Research Agent が情報収集に失敗 | 当該セクションのみ再試行（最大2回）。それでも失敗の場合はスキップし Writing Agent に「情報不足」と通知 |
| Phase 3 | Writing Agent が3回リトライ後も不合格 | Orchestrator がスコア履歴とフィードバックをユーザーへ報告。手動修正を提案 |
| Phase 3 | note-mcp の認証エラー | `note_login` を呼び出してブラウザ再認証を促す |
| Phase 3 | `create_draft` 失敗 | 1回リトライ後、エラー内容をユーザーへ報告 |
| Phase 3 | `publish_article` 失敗 | 下書きは保存済みと案内し、note.com 管理画面からの手動公開手順をユーザーへ案内 |

---

## 9. テンプレートシステム

### 9.1 設計思想

`templates/` フォルダは、本システムを**AI経営実装シリーズ以外の任意テーマに転用する**ための雛形セットである。

`input/`・`prompt/`・`output/` の各ファイルは AI 経営実装というドメインに特化した記述を含んでいる。テンプレートはそのドメイン固有の記述を `{{変数名}}` に置き換え、再利用可能な骨格のみを抽出したものである。

```
templates/
├── README.md                        ← テンプレート利用ガイド（変数定義一覧・転用例）
├── input_記事構成_template.md       ← 記事構成表の雛形（回・タイトル・結論・読者・章立て・次回接続）
├── prompt_記事作成_template.md      ← Writing Agent 用執筆プロンプトの雛形（34変数）
├── prompt_品質チェック_template.md  ← Quality Check Agent 用評価プロンプトの雛形（22変数）
└── output_記事構造_template.md      ← 完成記事の9章構成・文体基準の雛形
```

### 9.2 主要変数一覧

テンプレート全体で共通して使用される主要変数：

| 変数名 | 役割 | AI経営実装での値（例） |
|--------|------|----------------------|
| `{{SERIES_NAME}}` | シリーズ名 | AI経営実装シリーズ |
| `{{AUTHOR_PERSONA}}` | 著者ペルソナ | BtoBコンサルティング会社の編集責任者兼、経営層向けコンテンツマーケティングの専門家 |
| `{{TARGET_READERS}}` | 対象読者（列挙） | CEO、COO、CFO、DX担当役員、経営企画、事業部長 |
| `{{TARGET_READER_GROUP}}` | 対象読者の総称 | 経営層 |
| `{{TOPIC_DOMAIN}}` | テーマドメイン | AI組織実装・AI経営実装 |
| `{{ARTICLE_GOAL}}` | 記事の最終目的 | AI経営実装コンサルの相談・受注につなげること |
| `{{CORE_PROBLEM}}` | 中心的な問題設定 | 使っているのに成果が出ない |
| `{{IMPACT_LIST}}` | ビジネスインパクト例 | AI投資ROI / 機会損失 / 競争優位の喪失 など |
| `{{FRAMEWORK_EXAMPLES}}` | 独自フレームワーク例 | AI成果化成熟度診断 / 90日実装ロードマップ など |
| `{{BUSINESS_FUNCTIONS}}` | 具体的業務機能 | 営業 / 法務 / 経理 / 人事 / 問い合わせ対応 |
| `{{TRUSTED_SOURCES}}` | 信頼できる情報源 | BCG、PwC、JUAS、NRI、企業公式発表 |
| `{{CTA_GOOD_EXAMPLE}}` | 良いCTA例 | 初回診断では…（具体的な成果物を含む文） |
| `{{EPISODE_COUNT}}` | シリーズ全回数 | 10 |

評価スコアリングのうちドメイン依存の2項目:

| 変数名 | 役割 | AI経営実装での値（例） |
|--------|------|----------------------|
| `{{SCORING_ITEM_3_LABEL}}` | 評価項目3ラベル | 経営層への刺さり |
| `{{SCORING_ITEM_5_LABEL}}` | 評価項目5ラベル | 経営インパクト |

### 9.3 新プロジェクト立ち上げ手順（チャット UI 経由）

```
1. チャットに「記事を作成したい」と入力
2. Config Agent の 13問に答える（所要時間: 約5〜10分）
3. 設定サマリーを確認し、必要なら修正
4. 記事シリーズの全回分構成をチャットで入力
5. 「第N回を処理して」と指示して MAS を起動
```

**テンプレートファイルを直接編集する必要はありません。**
`templates/variables_definition.md` はシステム開発者が参照するものです。

### 9.4 転用例

| テーマ | AUTHOR_PERSONA | TARGET_READER_GROUP | ARTICLE_GOAL |
|--------|---------------|---------------------|--------------|
| **AI経営（既存）** | BtoBコンサルの編集責任者 | 経営層 | AI経営実装コンサルの受注 |
| BtoB SaaSマーケティング | SaaSマーケの専門家 | CMO・マーケ部長 | マーケ診断サービスの受注 |
| 中小企業の資金調達 | 中小企業向け財務コンサル | 中小企業経営者・CFO | 資金調達支援の相談受付 |
| HR Tech導入 | 組織開発・HR専門家 | CHRO・人事部長 | HR Tech導入支援の受注 |

---

## 10. ディレクトリ構成と各ファイルの役割

```
note/
├── input/                            ← 既存の AI 経営実装版ファイル（参照用）
│   └── 記事構成.docx                 ← AI経営実装10回分のシリーズ定義表（既存）
├── prompt/                           ← 既存の AI 経営実装版プロンプト（参照用）
│   ├── 記事作成プロンプト.docx        ← AI経営実装版の執筆指示（既存）
│   └── 品質チェックプロンプト.docx    ← AI経営実装版の評価観点（既存）
├── output/                           ← 完成記事サンプル（Writing Agent の文体基準として参照）
│   └── AI経営実装シリーズ_第1回.docx  ← AI経営実装版の完成記事サンプル（既存）
├── templates/                        ← 雛形セット（Config Agent と開発者が参照）
│   ├── README.md                     ← テンプレート利用ガイド（ユーザー向け説明）
│   ├── variables_definition.md       ← 変数定義マスタ（Config Agent が参照・13問の質問文・導出ルール）
│   ├── input_記事構成_template.md    ← 記事構成表の雛形
│   ├── prompt_記事作成_template.md   ← 記事作成プロンプトの雛形（13 CORE + 34 DERIVED 変数）
│   ├── prompt_品質チェック_template.md ← 品質チェックプロンプトの雛形
│   └── output_記事構造_template.md   ← 記事構造・文体基準の雛形（9章構成）
├── runtime/                          ← Config Agent が自動生成する実行用ファイル群 ★
│   ├── variables.json                ← CORE + DERIVED 全変数のキーバリュー
│   ├── input/
│   │   └── 記事構成.md               ← チャットで入力されたシリーズ全回分の構成表
│   ├── prompt/
│   │   ├── 記事作成プロンプト.md     ← 変数埋め込み済み（Writing Agent が参照）
│   │   └── 品質チェックプロンプト.md ← 変数埋め込み済み（Quality Check Agent が参照）
│   └── output/
│       └── 記事構造.md               ← 変数埋め込み済み（Writing Agent の文体基準）
├── note-mcp/                         ← MCP サーバー（Upload Agent が呼び出す）
│   └── src/note_mcp/
│       └── server.py                 ← note_check_auth・create_draft・publish_article を提供
└── 業務フロー及びシステム設計書.md    ← 本ドキュメント
```

> **ルール**: MAS の各エージェントは `runtime/` フォルダのファイルのみを参照する。`input/`・`prompt/`・`output/` の既存 docx ファイルは参照しない（AI経営実装版の既存資料として保存する）。

---

## 11. 処理対象のシリーズ構成（input/記事構成.docx より）

| 回 | 記事タイトル | 主要読者 |
|----|------------|---------|
| 第1回 | 日本企業のAI導入は、なぜ「使っているのに成果が出ない」のか | CEO・COO・DX担当役員・経営企画 |
| 第2回 | AI投資の成果は、なぜ利用率では測れないのか | CEO・CFO・経営企画・DX担当役員 |
| 第3回 | AIは個人の便利ツールではなく、業務プロセスを変える技術である | COO・事業部長・DX担当役員・業務改革責任者 |
| 第4回 | 縦割り組織はAIの価値を切り刻む | CEO・COO・事業部長・経営企画 |
| 第5回 | AIガバナンスは「禁止ルール」ではなく、競争力である | CEO・CIO・CISO・法務・コンプライアンス・DX担当役員 |
| 第6回 | PoCで終わる会社と、AIから学習する会社の違い | DX担当役員・事業部長・経営企画・PMO |
| 第7回 | AI人材不足という誤診——足りないのは専門家ではなく「翻訳者」である | CEO・CHRO・DX担当役員・事業部長 |
| 第8回 | AIで削減した時間は、どこへ消えたのか | CEO・COO・CFO・事業部長・人事責任者 |
| 第9回 | AIエージェントは「便利な自動化」ではなく、権限を持つ非人間社員である | CEO・CIO・CISO・COO・DX担当役員 |
| 第10回 | AIを中期経営計画にどう組み込むべきか | CEO・取締役・経営企画・CFO・DX担当役員 |
