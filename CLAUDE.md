# note記事自動作成・自動アップロード システム設計書

**バージョン**: 4.0 | **作成日**: 2026-05-09

---

## 1. システム概要

チャット UI でユーザーから変数を収集し、テンプレートに自動埋め込んだ上でマルチエージェントシステム（MAS）が記事を生成・品質チェック・note.com へアップロードする。13問に答えるだけで任意テーマ・任意読者の記事シリーズに転用できる設計とする。

### 処理フェーズ

| フェーズ | 担当エージェント | 概要 |
|---------|---------------|------|
| **Phase 0** 変数収集 | Config Agent | チャットで13問収集 → 約34変数を自動導出 → `runtime/` に展開 |
| **Phase 1〜2** 記事生成 | Parser / Research / Writing / Quality Check | `runtime/` を参照して記事生成・品質評価（スコア85点以上で合格） |
| **Phase 3** レビュー・公開 | Upload Agent + HITL×3 | 人間の承認を経て note.com に公開 |

### 設計上の前提

- **ファイル直接編集なし**: 変数はチャット UI で収集し、Config Agent がテンプレートに埋め込む
- **骨子はゼロから作らない**: 記事構成（タイトル・結論・章立て）はチャットで記入し `runtime/input/記事構成.md` に保存
- **1記事ずつ処理**: MAS は1回分の記事を対象として起動する
- **runtime/ のみ参照**: MAS の各エージェントは `runtime/` フォルダのファイルのみを参照する

---

## 2. システムアーキテクチャ

```
╔══════════════════════════════════════════════════════════════════╗
║               ユーザー（チャット UI）                             ║
║  Phase 0: 13問に回答  │  Phase 1〜2: 進捗確認  │  Phase 3: 承認  ║
╚══════════════════╤═══════════════════════════╤════════════════════╝
                   │ 変数入力                   │ HITL ①②③
                   ▼                           │
      ┌────────────────────────┐               │
      │     Config Agent       │               │
      │  CORE 13問収集         │               │
      │  DERIVED 34変数を導出  │               │
      │  templates/ → runtime/ │               │
      └────────────┬───────────┘               │
                   │ runtime/ 生成完了           │
                   ▼                           │
      ┌────────────────────────────────────────┴──────────┐
      │                  Orchestrator Agent                │
      └──┬─────────────┬───────────────────┬──────────────┘
         ↓             ↓                   ↓
   ┌──────────┐ ┌──────────────┐ ┌──────────────────────┐
   │ Parser   │ │  Research    │ │   Writing Agent      │
   │ Agent    │►│  Agent       │►│  runtime/prompt/ 参照│
   │ 構成解析 │ │ 章別並列収集 │ │  9章構成で執筆       │
   └──────────┘ └──────────────┘ └──────────┬───────────┘
                                             │ フィードバック（不合格時）
                                             ▼
                                ┌────────────────────────┐
                                │  Quality Check Agent   │
                                │  10項目×10点 = 100点   │
                                │  閾値: 85点            │
                                └────────────┬───────────┘
                                             │ ≥85点
                                        [HITL-①] 記事レビュー
                                             │ 承認
                                        [HITL-②] Upload承認
                                             │ 承認
                                        Upload Agent（note-mcp）
                                             ↓
                                        [HITL-③] 公開最終確認
                                             ↓
                                        note.com（公開）
```

---

## 3. エージェント定義

### 3.0 Config Agent（Phase 0 専任）

**役割**: チャット UI で変数を収集し、`runtime/` を生成する。

**責務**:
- `templates/variables_definition.md` を参照し、CORE 変数13問をグループ別に質問（詳細は同ファイル参照）
- 13問完了後、DERIVED 変数（約34個）を導出ルールに従い自動生成
- 設定サマリーをユーザーに提示し「確認 / 修正」を待つ
- `templates/` の4ファイルへ変数を一括置換して `runtime/` に保存
- 記事シリーズ全回分の構成をチャットで促し `runtime/input/記事構成.md` に保存
- 既存 `runtime/variables.json` がある場合は再利用を提案

**入力**: ユーザーのチャット入力 + `templates/`  
**出力**: `runtime/` フォルダ一式（variables.json + 実行用ファイル4種）

---

### 3.1 Orchestrator Agent

**役割**: 全体制御・エージェント間のデータ受け渡し・HITL ポイントの制御。

**責務**:
- `runtime/variables.json` の存在確認（Phase 0 完了チェック）
- ユーザーから「第N回」の指定を受け取り、各エージェントを起動・制御
- スコア閾値（85点）の判定と分岐処理
- HITL ポイントでのユーザー通知・待機・入力処理
- エラーハンドリング

---

### 3.2 Parser Agent

**役割**: `runtime/input/記事構成.md` から対象回の記事仕様を構造化して取り出す。

**責務**:
- 対象回（第N回）の6項目（回・タイトル・結論・主要読者・章立て・次回接続）を読み込む
- 章立てをセクション配列に分解した記事仕様 JSON を生成して Orchestrator へ渡す

---

### 3.3 Research Agent

**役割**: 記事仕様の各セクションに必要な情報・データ・事例を収集する。

**責務**:
- セクションごとに WebSearch で情報収集（セクション数分を**並列処理**）
- `runtime/variables.json` の `TRUSTED_SOURCES` に記載の情報源を優先取得
- 出典名・調査名・発表年を記録。一次情報未確認の事例には「未確認フラグ」を付与

---

### 3.4 Writing Agent

**役割**: 記事仕様・収集情報・プロンプトをもとに記事本文を執筆する。

**責務**:
- `runtime/prompt/記事作成プロンプト.md` の指示に全面準拠
- `runtime/output/記事構造.md` の文体・章構成を基準として踏襲
- 必須9章構成（タイトル → リード文 → 課題化 → 構造分解 → 独自フレーム → 実務例 → アクション → まとめ → CTA）で執筆
- Quality Check Agent のフィードバックを受けて再執筆（最大3回）

**文体ルール（`runtime/output/記事構造.md` より）**:

| 使って良い表現 | 避けるべき表現 |
|-------------|--------------|
| 「〜の構造を解剖する」「〜に踏み込む」 | 「圧倒的多数」「必ず〜できる」「劇的に改善」 |
| 「注目すべきは数字ではなく、そこに至る設計だ」 | 「本当の理由」「お気軽にご相談ください」 |

---

### 3.5 Quality Check Agent

**役割**: `runtime/prompt/品質チェックプロンプト.md` の指示に基づき記事を評価する。

**責務**:
- 下記10項目を10点満点でスコアリングし、加点・減点ルールを適用（合計100点）
- スコア < 85点 → フィードバック付きで Writing Agent に差し戻し（最大3回）
- スコア ≥ 85点 → 評価レポート（強み・弱点・赤入れ例・CTA改善案・最優先修正リスト）を生成

**評価10項目**:

| # | 項目 | 主な観点 |
|---|------|---------|
| 1 | タイトル | 課題と成果が明確でクリックしたくなるか |
| 2 | 冒頭の掴み | 読者の痛みを具体的に突いているか |
| 3 | ターゲット読者への刺さり | 対象役職それぞれが自分ごととして読めるか |
| 4 | 問題設定の鋭さ | 表面的でなく構造的な問題を突いているか |
| 5 | ビジネスインパクト | ROI・機会損失・競争優位等に接続しているか |
| 6 | 独自性 | 独自フレーム・診断軸があり相談したくなるか |
| 7 | 具体性 | 業務工程・KPI・判断基準まで具体的か |
| 8 | ファクト信頼性 | 出典が正確で断定と解釈が分かれているか |
| 9 | 読みやすさ | 章タイトルだけで論旨が追え短時間で理解できるか |
| 10 | 受注・行動導線 | 本文とCTAが自然につながり初回診断の価値が明確か |

---

### 3.6 Upload Agent

**役割**: 人間の承認後に note-mcp を通じて記事を投稿する。

**責務**:
- 認証確認（`note_check_auth`）→ 下書き保存（`create_draft`）→ プレビューURL提示
- HITL-③ でユーザーが「公開する」を選択した場合のみ `publish_article` を実行

---

## 4. 業務フロー

```
━━━━━━━━━━━━━━━━━━━━━━━━ Phase 0: 変数収集 ━━━━━━━━━━━━━━━━━━━━━━━━

[ユーザー] 「記事を作成したい」と入力
    ↓
[Config Agent] runtime/variables.json の有無を確認
    ├─ あり → 「前回の設定を使いますか？」 YES → Phase 1 へ
    └─ なし → CORE 変数 13問をグループ別に質問
                 G1: 基本情報（シリーズ名・回数・テーマ）
                 G2: ターゲット（著者ペルソナ・対象読者・総称）
                 G3: 目的・ゴール（記事目的・サービス名・核心問題）
                 G4: コンテンツ設計（フレームワーク・業務機能・情報源）
                 G5: CTA（初回診断で提供する価値）
    ↓
[Config Agent] DERIVED 変数（約34個）を自動導出 → 設定サマリーを提示
    ├─ [1] このまま進む
    └─ [2] 特定の変数を修正する
    ↓ 確認
[Config Agent] templates/ → runtime/ に一括置換して保存
[Config Agent] 記事シリーズ全回分の構成をチャットで記入促す
               → runtime/input/記事構成.md に保存
    ↓ セットアップ完了

━━━━━━━━━━━━━ Phase 1〜2: 記事生成（1回分を指定して起動） ━━━━━━━━━━━━━

[ユーザー] 「第N回を処理して」と指示
    ↓
[Orchestrator] runtime/variables.json 確認 → note_check_auth
    ↓
[Parser Agent] runtime/input/記事構成.md → 第N回の記事仕様 JSON を生成
    ↓
[Research Agent] セクションごとに並列 WebSearch → 情報パッケージ統合
    ↓
[Writing Agent] runtime/prompt/記事作成プロンプト.md + 記事仕様 + 収集情報
                → 9章構成で Markdown 記事を執筆
    ↓
[Quality Check Agent] runtime/prompt/品質チェックプロンプト.md
                      → 10項目スコアリング + 加減点
    ↓
  スコア < 85点 → フィードバック付きで Writing Agent へ差し戻し（最大3回）
                  3回失敗 → ユーザーへ報告・終了
  スコア ≥ 85点 ↓

━━━━━━━━━━━━━━━━━━━━━ Phase 3: レビュー・公開 ━━━━━━━━━━━━━━━━━━━━━

╔══════════════════════════════════════════════╗
║  HITL-①  記事レビュー                       ║
║  スコア・評価内訳・強み・弱点・              ║
║  赤入れ例・記事全文を提示                   ║
║  [承認 / 修正依頼 / 却下]                   ║
╚══════════════════════════════════════════════╝
    ↓ 修正依頼 → Writing Agent で再執筆 → Quality Check へ戻る
    ↓ 承認

╔══════════════════════════════════════════════╗
║  HITL-②  アップロード承認                   ║
║  タイトル・タグ・公開範囲を提示             ║
║  「note に下書き保存しますか？」            ║
║  [はい / いいえ]                            ║
╚══════════════════════════════════════════════╝
    ↓ はい

[Upload Agent] note_check_auth → create_draft → プレビューURL提示

╔══════════════════════════════════════════════╗
║  HITL-③  公開最終確認                       ║
║  「公開しますか？下書きのままにしますか？」  ║
║  [公開する / 下書きのまま]                  ║
╚══════════════════════════════════════════════╝
    ↓ 公開する

[Upload Agent] publish_article → 公開済みURL報告 → 完了
```

> **HITL 原則**: 3つのポイントすべてで Claude が自律的にアクションを取らない。必ず人間の明示的な入力を待機する。

---

## 5. MCP 連携（note-mcp）

| ツール | 使用タイミング | 用途 |
|--------|-------------|------|
| `note_check_auth` | Phase 1 起動時・Upload 直前 | 認証状態の確認 |
| `note_login` | 未認証時 | ブラウザログイン |
| `create_draft` | HITL-② 承認後 | 下書き保存 |
| `publish_article` | HITL-③「公開する」選択後のみ | 記事公開 |

**アップロード時のデータマッピング**:

| note-mcp パラメータ | 取得元 |
|--------------------|-------|
| `title` | 記事仕様JSON の `title` |
| `body` | Writing Agent が生成した Markdown 本文 |
| `tags` | 記事仕様JSON の `target_readers` から生成 |
| `status` | `"draft"` → HITL-③ 後に `publish_article` |

---

## 6. エラーハンドリング

| フェーズ | エラーケース | 対処 |
|---------|------------|------|
| Phase 0 | 途中で回答をやめた | `runtime/variables.json` に途中まで保存済み。次回「続きから」として再開できる旨を通知 |
| Phase 0 | DERIVED 変数の導出に失敗 | デフォルト値で補完し、サマリー画面でユーザーに手動修正を促す |
| Phase 0 | 置換後に未置換の `{{変数名}}` が残った | 置換漏れ変数を列挙してユーザーに通知 |
| Phase 1 | `runtime/variables.json` が存在しない | Phase 0 を先に実行するよう促す |
| Phase 1 | Parser Agent が対象回を見つけられない | 処理停止。ユーザーに回数の確認を促す |
| Phase 2 | Research Agent の情報収集失敗 | 当該セクションのみ再試行（最大2回）。失敗時はスキップし「情報不足」と通知 |
| Phase 2 | Writing Agent が3回リトライ後も不合格 | スコア履歴とフィードバックをユーザーへ報告。手動修正を提案 |
| Phase 3 | note-mcp の認証エラー | `note_login` でブラウザ再認証を促す |
| Phase 3 | `create_draft` 失敗 | 1回リトライ後、エラー内容をユーザーへ報告 |
| Phase 3 | `publish_article` 失敗 | 下書きは保存済みと案内し、note.com 管理画面からの手動公開手順を案内 |

---

## 7. ファイル構成

```
note/
├── sample/                           ← AI経営実装版のサンプルファイル（参照用）
│   ├── input/記事構成.docx
│   ├── prompt/記事作成プロンプト.docx
│   ├── prompt/品質チェックプロンプト.docx
│   └── output/AI経営実装シリーズ_第1回.docx
│
├── templates/                        ← 雛形セット（Config Agent が参照）
│   ├── README.md                     ← テンプレート利用ガイド
│   ├── variables_definition.md       ← 変数定義マスタ（13問の質問文・DERIVED 導出ルール）
│   ├── input_記事構成_template.md
│   ├── prompt_記事作成_template.md   ← 13 CORE + 34 DERIVED 変数
│   ├── prompt_品質チェック_template.md
│   └── output_記事構造_template.md   ← 9章構成・文体基準
│
├── runtime/                          ← Config Agent が自動生成（.gitignore 対象）
│   ├── variables.json                ← CORE + DERIVED 全変数
│   ├── input/記事構成.md             ← チャットで記入したシリーズ構成表
│   ├── prompt/記事作成プロンプト.md  ← 変数埋め込み済み（Writing Agent 参照）
│   ├── prompt/品質チェックプロンプト.md ← 変数埋め込み済み（QC Agent 参照）
│   └── output/記事構造.md            ← 変数埋め込み済み（Writing Agent 文体基準）
│
├── note-mcp/                         ← MCP サーバー（Upload Agent が呼び出す）
│   └── src/note_mcp/server.py
│
└── CLAUDE.md                         ← 本ドキュメント
```

### テンプレートの転用手順（新テーマへの適用）

```
1. チャットに「記事を作成したい」と入力
2. Config Agent の 13問に答える（所要: 約5〜10分）
3. 設定サマリーを確認し、必要なら修正
4. 記事シリーズの全回分構成をチャットで入力
5. 「第N回を処理して」と指示して MAS を起動
```

転用例:

| テーマ | TARGET_READER_GROUP | ARTICLE_GOAL |
|--------|---------------------|--------------|
| AI経営（既存） | 経営層 | AI経営実装コンサルの受注 |
| BtoB SaaSマーケティング | CMO・マーケ部長 | マーケ診断サービスの受注 |
| 中小企業の資金調達 | 中小企業経営者・CFO | 資金調達支援の相談受付 |
| HR Tech導入 | CHRO・人事部長 | HR Tech導入支援の受注 |
